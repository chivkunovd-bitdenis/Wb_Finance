# Note
# Этот файл оставлен как вспомогательный.
# Основная оркестрация задаётся корневым `AGENTS.md` (в соответствии с документацией Cursor).
# Не пытайся поддерживать два независимых routing-дерева одновременно.


# Полная генерация товара (ИИ) — атомарные задачи

**План планировщика (контракты + порядок работ):** [`PRODUCT_GEN_PLAN.md`](./PRODUCT_GEN_PLAN.md) — **читать перед кодом**.

Статус: **готово к Фазе 1 (Builder)**. Реализацию вести **строго по строкам сверху вниз** внутри фазы (или явно помечать зависимости).

Легенда: `[ ]` не начато · `[~]` в работе · `[x]` готово · **DoD** = критерий готовности.

---

## Фаза 0 — Зафиксировать контракт (без новой бизнес-логики)

| ID | Задача | DoD |
|----|--------|-----|
| PG-0.1 | Зафиксировать **Scenario contract** + **Flow trace** (монолит ↔ image-service ↔ WB) в одном md рядом с этим файлом | ✅ см. **§2–§3** в [`PRODUCT_GEN_PLAN.md`](./PRODUCT_GEN_PLAN.md) |
| PG-0.2 | Зафиксировать **UX/UI Contract** мастера (шаги, закрытие после «Создать», карточка задачи, lightbox фото) | ✅ см. **§4** в [`PRODUCT_GEN_PLAN.md`](./PRODUCT_GEN_PLAN.md) |
| PG-0.3 | Решить судьбу уже созданного **скелета** `wb_image_pipeline_service/` (оставить как заготовку / убрать до старта) | ✅ **Оставить** заготовку под PG-3; см. **§1** плана |

---

## Фаза 1 — Доступ и «пустой» каркас в монолите

| ID | Задача | DoD |
|----|--------|-----|
| PG-1.1 | Backend: все эндпоинты фичи за **`require_admin`** (`user.is_admin`) | [x] 403 для не-админа; pytest на контракт |
| PG-1.2 | Модель + миграция: **черновик/задача полной генерации** (статус, `run_id` внешнего сервиса nullable, поля под SEO/цены/размеры/артикул/бренд — минимально необходимые) | [x] Alembic `9e1f2a3b4c5d`; модель `ProductGenerationJob` |
| PG-1.3 | API: `POST/GET/PATCH` список+деталка черновика (без реальных вызовов OpenAI/WB) | [x] `tests/test_product_generation_api.py` |
| PG-1.4 | Frontend: кнопка на вкладке **ИИ** только для админа; пустой мастер или заглушка «скоро» по флагу | [x] `AiModule.jsx` + `api.js`; флаг `VITE_PRODUCT_GEN_UI_STUB=1` |

---

## Фаза 2 — Форма и фоновый старт (без WB publish)

| ID | Задача | DoD |
|----|--------|-----|
| PG-2.1 | UI: мастер по шагам (**B**): референсы (multi-file), текст, габариты отдельными полями, цена, артикул, **наименование**, **бренд**, таблица размеров **techSize + wbSize** (шаблон **B**) | [x] `AiModule.jsx`: шаги 1–3, валидация; `POST /jobs` + `POST .../references` (PG-2.2) |
| PG-2.2 | Загрузка файлов: сохранение в хранилище **монолита** или передача в сервис по контракту (зафиксировать в PG-0.1) + `asset_id` в черновике | [x] `POST/GET .../references`, `product_generation_assets`, `reference_paths_json` с `asset_id`; Docker: `PRODUCT_GENERATION_REFERENCES_DIR` на `/app/data/...`; pytest |
| PG-2.3 | Последний шаг **«Создать»**: создаётся задача, **фон** запускается, форма закрывается; в списке задач статус «В процессе» | [x] `POST /jobs/{id}/start`, Celery `product_generation_pipeline_stub`, pytest; UI вызывает start после upload; опционально Playwright `e2e/product-generation-wizard.spec.js` (admin + `E2E_*`) |
| PG-2.4 | Категория WB: **не обязательна** в форме; при отсутствии — не блокируем черновик (поведение зафиксировать в контракте) | [x] Опциональное `wb_subject_id` в API + мастере; план §2/§5; pytest |

---

## Фаза 3 — Микросервис: очередь и run

| ID | Задача | DoD |
|----|--------|-----|
| PG-3.1 | Своя БД сервиса: схема `runs`, `steps`, `assets` (минимум) + миграции в папке сервиса | [x] Таблицы `wip_runs` / `wip_steps` / `wip_assets`, Alembic `a1b2c3d4e501`, entrypoint `upgrade`, volume `wip_data:/data`, pytest |
| PG-3.2 | Celery: цепочка-заглушка `run_created → step_done` с реальным Redis | [x] Задачи `wb_image_pipeline.run_created` / `wb_image_pipeline.step_done`, Redis в `docker-compose.example`, идемпотентные обновления `wip_*`, pytest (eager + двойной вызов) |
| PG-3.3 | HTTP: `POST /internal/v1/runs`, `GET /internal/v1/runs/{id}` (статус + метаданные) | [x] `app/api/internal_runs.py`, сервис `internal_runs_service`, Bearer `WIP_INTERNAL_HMAC_SECRET`, pytest `test_internal_runs_http.py`, README |
| PG-3.4 | Связка монолит ↔ сервис: создание run при «Создать»; сохранение `run_id`; поллинг статуса в карточке задачи | [x] `PRODUCT_GEN_IMAGE_PIPELINE_*`, `product_generation_image_pipeline.py`, старт/обогащение list+get, pytest с моком httpx; фронт: колонка + poll 4s |
| PG-3.5 | **mTLS** или зафиксированный промежуточный режим (только private network + HMAC) — как в `docs/mtls.md` | Инструкция для прода; dev не блокируется |

---

## Фаза 4 — LLM: структуризация и промпты

| ID | Задача | DoD |
|----|--------|-----|
| PG-4.1 | Воркер: вызов OpenAI **structure** модель из env (`WIP_OPENAI_MODEL_STRUCTURE=gpt-4.1-mini`); ответ JSON: SEO + **4** промпта главного кадра (стили зафиксированы в промпт-шаблоне) | Парсинг ошибок → человеческое сообщение в шаге |
| PG-4.2 | Воркер: генерация **4** изображений (модель из `WIP_OPENAI_IMAGE_MODEL`); запись в `assets` + на диск volume сервиса | Idempotency по `step_id` где возможно |
| PG-4.3 | Монолит: прокси **GET** картинки для UI (реальные фото в карточке + **lightbox** увеличение) | Не отдаём порт сервиса в браузер без авторизации |
| PG-4.4 | UI: выбор **одного** главного фото → вызов API → сервис получает событие и ставит следующий этап | Статус задачи обновляется |
| PG-4.5 | Воркер: **8** промптов серии (`WIP_OPENAI_MODEL_PROMPT_PACK=gpt-4.1`); сохранение текстов в БД сервиса | Промпты видны в логах/админ-дебаг при необходимости |
| PG-4.6 | Воркер: **8** изображений серии; галочки выбора в UI (состояние в монолите или сервисе — зафиксировать в PG-0.1) | Можно выбрать подмножество |

---

## Фаза 5 — Публикация WB

| ID | Задача | DoD |
|----|--------|-----|
| PG-5.1 | Клиент WB: `POST content/v2/barcodes` `count = число размеров` | Обработка `error`/`errorText`; pytest с моком |
| PG-5.2 | Клиент WB: `POST content/v2/cards/upload` с полями из контракта; **без** `characteristics` на первом этапе (**D**) | При отказе WB — **человекочитаемая** ошибка в UI + статус задачи (**A**) |
| PG-5.3 | UI: редактируемый SEO-текст + цена + кнопка **«Опубликовать»** + **«Повторить публикацию»** при ошибке | Не теряем черновик |
| PG-5.4 | Сохранение ответа WB (например идентификаторы, если приходят) при успехе (**A**) | Лог/поле в черновике |

---

## Фаза 6 — Наблюдаемость и уборка

| ID | Задача | DoD |
|----|--------|-----|
| PG-6.1 | TTL **14 дней** на файлы генерации после успешной публикации (**A+B**) | Celery beat или cron в сервисе; не удалять до подтверждения publish |
| PG-6.2 | Лимиты генераций: пока **нет** (**13.C**) — задокументировать риск в README фичи | — |

---

## Зависимости (кратко)

```
PG-0.* → PG-1.* → PG-2.*
PG-0.* → PG-3.*; PG-1.2 → PG-3.4
PG-3.* → PG-4.*
PG-1.* + PG-4.6 → PG-5.*
PG-5.4 → PG-6.1
```

---

## Честный статус реализации на сейчас

- **Фаза 0:** закрыта планом [`PRODUCT_GEN_PLAN.md`](./PRODUCT_GEN_PLAN.md).
- **Код ветки «продукт» (монолит):** **PG-1–2.4** закрыты; **PG-3.4** — опциональная связка с image-сервисом (`PRODUCT_GEN_IMAGE_PIPELINE_*`, поле `image_pipeline`, поллинг UI); дальше по продукту — **PG-4*** (LLM в воркере) и **PG-5** (WB); **PG-3.5** — усиление связки (mTLS).
- **Папка `wb_image_pipeline_service/`:** **PG-3.1–3.4** — БД, Celery-заглушка, HTTP runs, связка с монолитом (`PRODUCT_GEN_IMAGE_PIPELINE_*` + поллинг); следующий шаг — **PG-3.5** (mTLS / прод-HMAC).
